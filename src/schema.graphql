type Query {
  info: String!
}

type Content {
  id: ID! @unique
  contentType: String
  description: String
  media: String
  published: Boolean! @default(value: "true")
}

type Settings {
  id: ID! @unique
}
